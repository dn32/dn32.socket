using System;

namespace dn32.socket.Compartilhado
{
    internal static class Utilitario
    {
        internal static void MensagemDeDebugInterno(string mensagem)
        {
            Console.WriteLine(mensagem);
        }
    }
}